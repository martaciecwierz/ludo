plugins {
    id 'java'
}

allprojects {
    group = 'pl.ludo'
    version = '0.0.1-SNAPSHOT'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(25)
        }
    }
}

subprojects {
    repositories {
        mavenCentral()
    }
}